#include <stdio.h>
/**
 * print_alphabet_x10 - prints lowercase alphabets 10 times
 * Return: return zero
*/
void print_alphabet_x10(void)
{
	int x;
	char y;

	for (x = 0; x > 10; x++)
	{
		for (y = a; y <= z; y++)
		putchar("y\n");
	}
	putchar("\n");
	return (0);
}
